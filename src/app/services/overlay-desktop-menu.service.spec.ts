import { TestBed } from '@angular/core/testing';

import { OverlayDesktopMenuService } from './overlay-desktop-menu.service';

describe('OverlayDesktopMenuService', () => {
  let service: OverlayDesktopMenuService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(OverlayDesktopMenuService);
  });

  // it('should be created', () => {
  //   expect(service).toBeTruthy();
  // });
});
