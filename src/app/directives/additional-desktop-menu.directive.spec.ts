import { AdditionalDesktopMenuDirective } from './additional-desktop-menu.directive';

describe('ViewSizeDesktopItemsDirective', () => {
  it('should create an instance', () => {
    const directive = new AdditionalDesktopMenuDirective();
    expect(directive).toBeTruthy();
  });
});
