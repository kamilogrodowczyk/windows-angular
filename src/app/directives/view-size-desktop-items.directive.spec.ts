import { ViewSizeDesktopItemsDirective } from './view-size-desktop-items.directive';

describe('ViewSizeDesktopItemsDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewSizeDesktopItemsDirective();
    expect(directive).toBeTruthy();
  });
});
